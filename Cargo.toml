[package]
name = "exif-tool"
version = "0.1.0"
edition = "2024"
description = "A GPUI desktop utility that renames media files by capture time"
publish = false

[dependencies]
chrono = "0.4.42"
gpui = "0.2.2"
nom-exif = "3.6.2"

[dev-dependencies]
tempfile = "3.23.0"

[profile.release]
lto = "thin"
strip = true
